#!/usr/bin/env bash
set -euo pipefail

pnpm -r format
pnpm -r lint
pnpm -r typecheck
pnpm -r test
pnpm -r build

uv run ruff check .
uv run pytest

cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
