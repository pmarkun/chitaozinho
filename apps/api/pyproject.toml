[project]
name = "chitaozinho-api"
version = "0.1.0"
requires-python = ">=3.13"
license = "Apache-2.0"
dependencies = [
  "aiosqlite>=0.21.0",
  "alembic>=1.16.5",
  "boto3>=1.40.0",
  "chitaozinho-protocol",
  "fastapi>=0.116.1",
  "opentimestamps-client>=0.7.2",
  "pydantic-settings>=2.10.1",
  "psycopg[binary]>=3.2.9",
  "sqlalchemy>=2.0.43",
  "uvicorn>=0.35.0",
]

[dependency-groups]
dev = [
  "httpx>=0.28.1",
  "pytest>=8.4.1",
]

[tool.uv.sources]
chitaozinho-protocol = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/chitaozinho_api"]
